# 백엔드 404 오류 해결 가이드

## 🔴 문제: 백엔드 URL 접속 시 404 오류

모든 백엔드 테스트 URL이 404를 반환합니다.

---

## 🔍 원인 진단

### 가능한 원인들:

1. **백엔드 서버가 실행되지 않음**
2. **백엔드 URL이 잘못됨**
3. **라우트 설정 문제**
4. **Root Directory 설정 문제**

---

## ✅ 단계별 확인 방법

### 1단계: 백엔드 서비스 상태 확인

1. Render 대시보드 → `order-app-backend` 서비스
2. **상태 확인**:
   - "Live" 상태인지 확인
   - "Building" 또는 "Deploying" 상태면 배포 대기
   - "Failed" 상태면 로그 확인

3. **서비스 URL 확인**:
   - 상단에 표시된 URL 확인
   - 예: `https://order-app-backend-xxxx.onrender.com`
   - 이 URL을 정확히 기록하세요

---

### 2단계: 백엔드 로그 확인

1. Render 대시보드 → `order-app-backend` 서비스
2. **Logs** 탭 클릭
3. 최근 로그 확인:

**정상적인 로그**:
```
🚀 서버가 포트 10000에서 실행 중입니다.
🚀 환경: production
```

**에러 로그 확인**:
- 데이터베이스 연결 에러
- 모듈을 찾을 수 없음 에러
- 포트 바인딩 에러

---

### 3단계: Root Directory 확인

1. Render 대시보드 → `order-app-backend` 서비스
2. **Settings** 탭 클릭
3. **Root Directory** 확인:
   - `server`로 설정되어 있어야 함
   - 루트(`/`)로 설정되어 있으면 수정 필요

---

### 4단계: Start Command 확인

1. Render 대시보드 → `order-app-backend` 서비스
2. **Settings** 탭 클릭
3. **Start Command** 확인:
   - `npm start` 또는 `node src/index.js`로 설정되어 있어야 함
   - `node index.js`로 되어 있으면 수정 필요

---

### 5단계: 실제 백엔드 URL로 테스트

Render 대시보드에서 확인한 실제 백엔드 URL로 테스트:

1. **루트 경로 테스트**:
   ```
   https://order-app-backend-xxxx.onrender.com/
   ```
   - 정상: API 엔드포인트 목록 JSON 반환

2. **헬스 체크 테스트**:
   ```
   https://order-app-backend-xxxx.onrender.com/health
   ```
   - 정상: `{"success":true,"message":"서버가 정상적으로 실행 중입니다."}`

3. **API 엔드포인트 테스트**:
   ```
   https://order-app-backend-xxxx.onrender.com/api/menus
   ```
   - 정상: 메뉴 데이터 JSON 반환
   - 404: 라우트 설정 문제 또는 서버 미실행

---

## 🔧 해결 방법

### 문제 1: 백엔드 서버가 실행되지 않음

**증상**: 모든 URL이 404 또는 연결 실패

**해결**:
1. Render 대시보드 → `order-app-backend` 서비스
2. **Logs** 탭에서 에러 확인
3. **Settings** 탭에서 설정 확인:
   - Root Directory: `server`
   - Start Command: `npm start`
4. 수동 재배포: "Manual Deploy" → "Deploy latest commit"

---

### 문제 2: Root Directory 설정 오류

**증상**: 모듈을 찾을 수 없음 에러

**해결**:
1. Render 대시보드 → `order-app-backend` 서비스
2. **Settings** 탭
3. **Root Directory** 수정: `server`
4. 저장 후 재배포

---

### 문제 3: Start Command 설정 오류

**증상**: 서버가 시작되지 않음

**해결**:
1. Render 대시보드 → `order-app-backend` 서비스
2. **Settings** 탭
3. **Start Command** 수정: `npm start`
4. 저장 후 재배포

---

### 문제 4: 라우트 설정 문제

**증상**: 루트(`/`)는 작동하지만 `/api/menus`는 404

**확인**:
1. `server/src/index.js` 파일 확인
2. 라우트가 올바르게 설정되어 있는지 확인:
   ```javascript
   app.use('/api/menus', menusRouter);
   app.use('/api/orders', ordersRouter);
   ```

---

## 📋 올바른 Render 설정

### 백엔드 서비스 설정

```
Name: order-app-backend
Region: <데이터베이스와 같은 지역>
Branch: main
Root Directory: server          ⚠️ 중요!
Runtime: Node
Build Command: npm install
Start Command: npm start         ⚠️ 중요!
```

### 환경 변수

```
NODE_ENV = production
PORT = 10000

DB_HOST = <Render 데이터베이스 Host>
DB_PORT = 5432
DB_NAME = order_app
DB_USER = <Render 데이터베이스 User>
DB_PASSWORD = <Render 데이터베이스 Password>

FRONTEND_URL = https://order-app-frontend-0g6j.onrender.com
```

---

## 🔍 디버깅 체크리스트

### 백엔드 서비스 확인
- [ ] 서비스가 "Live" 상태
- [ ] Root Directory: `server` 설정
- [ ] Start Command: `npm start` 설정
- [ ] Build Command: `npm install` 설정
- [ ] 로그에 "서버가 포트 10000에서 실행 중입니다" 메시지 확인

### URL 테스트
- [ ] 루트 경로(`/`) 접속 가능
- [ ] 헬스 체크(`/health`) 접속 가능
- [ ] API 엔드포인트(`/api/menus`) 접속 가능

### 환경 변수
- [ ] 모든 환경 변수 설정 완료
- [ ] 데이터베이스 연결 정보 정확함

---

## 💡 빠른 테스트 방법

### 1. Render 대시보드에서 확인

1. `order-app-backend` 서비스 선택
2. 상단에 표시된 URL 확인
3. "Open Live URL" 버튼 클릭 (있다면)

### 2. 브라우저에서 직접 접속

실제 백엔드 URL로 접속:
```
https://order-app-backend-xxxx.onrender.com/
```

**정상 응답 예시**:
```json
{
  "success": true,
  "message": "커피 주문 앱 API 서버",
  "version": "1.0.0",
  "endpoints": {
    "health": "/health",
    "menus": {
      "getAll": "GET /api/menus",
      ...
    }
  }
}
```

---

## 🚨 여전히 404가 발생하면

1. **백엔드 로그 전체 확인**
   - Render 대시보드 → Logs 탭
   - 에러 메시지 복사

2. **서비스 설정 재확인**
   - Root Directory
   - Start Command
   - Build Command

3. **GitHub 저장소 확인**
   - 최신 코드가 푸시되었는지 확인
   - `server/` 폴더 구조 확인

4. **수동 재배포**
   - "Manual Deploy" → "Deploy latest commit"

---

## 📞 추가 도움이 필요하면

다음 정보를 확인하세요:

1. **백엔드 로그** (전체)
2. **서비스 설정 스크린샷** (Settings 탭)
3. **실제 백엔드 URL**
4. **브라우저에서 접속한 결과** (스크린샷)

이 정보를 바탕으로 추가 진단이 가능합니다.

