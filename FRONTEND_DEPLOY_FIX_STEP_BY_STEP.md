# 프론트엔드 배포 오류 해결 - 단계별 가이드

## 🔴 현재 오류

```
==> Publish directory npm start does not exist!
==> Build failed 😞
```

**원인**: Render 대시보드에서 **Publish Directory** 필드에 `npm start`가 입력되어 있습니다.

---

## ✅ 해결 방법 (단계별)

### 1단계: Render 대시보드 접속

1. https://dashboard.render.com 접속
2. 로그인

### 2단계: 기존 서비스 확인 및 삭제

1. `order-app-ui` 서비스 찾기
2. **Settings** 탭 클릭
3. **"Publish Directory"** 필드 확인
   - 현재 값이 `npm start` 또는 비슷한 값으로 되어 있을 것입니다
4. **서비스 삭제** (또는 아래 3단계에서 수정)

### 3단계: Static Site 설정 수정

**옵션 A: 기존 서비스 수정 (가능한 경우)**

1. **Settings** 탭에서:
   - **Publish Directory**: `dist` 입력
   - **Build Command**: `npm install && npm run build` 확인
   - **Start Command**: 비워두기 (Static Site에는 없음)
2. **"Save Changes"** 클릭

**옵션 B: 새로 생성 (권장 - 더 확실함)**

1. 기존 서비스 삭제
2. **"New +"** 버튼 클릭
3. **"Static Site"** 선택 (⚠️ Web Service 아님!)
4. GitHub 저장소 연결: `yihyunsong777/order-app`

5. **서비스 설정 입력**:
   ```
   Name: order-app-ui
   Branch: main
   Root Directory: ui
   Build Command: npm install && npm run build
   Publish Directory: dist          ⚠️ 이것이 핵심!
   ```

   **중요 체크리스트**:
   - ✅ Type: **Static Site** (Web Service 아님!)
   - ✅ Root Directory: `ui`
   - ✅ Build Command: `npm install && npm run build`
   - ✅ **Publish Directory: `dist`** (⚠️ `npm start` 아님!)
   - ✅ Start Command: **없음** (비워두기)

6. **환경 변수 추가** (Environment Variables):
   ```
   VITE_API_URL = https://order-app-server.onrender.com
   ```
   - 백엔드 서비스 이름이 다르면 해당 URL로 변경
   - 프로토콜(`https://`) 포함 필수

7. **"Create Static Site"** 클릭

### 4단계: 배포 확인

1. 배포 로그 확인:
   - "✓ built in X.XXs" 메시지 확인
   - "Build successful" 메시지 확인
   - **"Publish directory npm start does not exist!" 오류가 사라져야 함**

2. 서비스 상태:
   - "Live" 상태 확인
   - URL 확인: `https://order-app-ui.onrender.com`

---

## 📸 설정 스크린샷 가이드

### 올바른 Static Site 설정

```
┌─────────────────────────────────────┐
│ Service Type: Static Site           │
├─────────────────────────────────────┤
│ Name: order-app-ui                  │
│ Branch: main                        │
│ Root Directory: ui                  │
│ Build Command: npm install && npm run build │
│ Publish Directory: dist    ← 핵심! │
│                                     │
│ [Environment Variables]             │
│ VITE_API_URL = https://...          │
└─────────────────────────────────────┘
```

### ❌ 잘못된 설정 (현재 상태)

```
┌─────────────────────────────────────┐
│ Service Type: Static Site           │
├─────────────────────────────────────┤
│ Publish Directory: npm start  ← 잘못됨! │
└─────────────────────────────────────┘
```

---

## 🔍 문제 진단

### 현재 상황 확인

Render 로그에서:
- ✅ 빌드는 성공: `✓ built in 1.80s`
- ❌ Publish Directory 오류: `Publish directory npm start does not exist!`

**의미**: 
- 빌드는 정상적으로 완료되었습니다
- 하지만 Render가 `dist` 폴더 대신 `npm start` 폴더를 찾으려고 합니다
- → **Publish Directory 설정이 잘못되었습니다**

---

## 💡 추가 팁

### Vite 빌드 출력 확인

빌드 로그를 보면:
```
dist/index.html                   0.45 kB
dist/assets/index-DLSQ-oQ5.css   11.22 kB
dist/assets/index-CIWU2ZgO.js   207.52 kB
```

이것은 `dist` 폴더에 빌드된 파일이 있다는 뜻입니다.
따라서 **Publish Directory는 반드시 `dist`여야 합니다**.

### 환경 변수 확인

빌드 시점에 환경 변수가 주입되므로:
1. Render 대시보드 → Environment 탭
2. `VITE_API_URL` 확인
3. 백엔드 URL이 정확한지 확인

---

## ✅ 최종 체크리스트

배포 전:
- [ ] Render에서 Static Site로 생성/수정
- [ ] Publish Directory: `dist` 설정 (⚠️ `npm start` 아님!)
- [ ] Root Directory: `ui` 설정
- [ ] Build Command: `npm install && npm run build` 설정
- [ ] Start Command: 없음 (비워두기)
- [ ] 환경 변수 `VITE_API_URL` 설정

배포 후:
- [ ] 빌드 성공 로그 확인
- [ ] "Publish directory npm start does not exist!" 오류 없음
- [ ] 서비스가 "Live" 상태
- [ ] 프론트엔드 URL 접속 가능
- [ ] 메뉴 목록 표시 확인

---

## 🆘 여전히 문제가 발생하면

1. **Render 대시보드에서 정확한 설정 확인**:
   - Settings 탭 → Publish Directory 필드 스크린샷 확인
   - 값이 정확히 `dist`인지 확인 (공백 없이)

2. **서비스 타입 확인**:
   - Static Site인지 Web Service인지 확인
   - Web Service라면 삭제하고 Static Site로 재생성

3. **로그 전체 확인**:
   - 에러 메시지 전체 확인
   - 빌드 과정에서 다른 오류가 있는지 확인

